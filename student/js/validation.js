function showQueryMessage(){const p=new URLSearchParams(window.location.search);const box=document.getElementById("message");if(!box)return;if(p.has("error")){box.className="message-error";box.textContent=p.get("error")}if(p.has("success")){box.className="message-success";box.textContent=p.get("success")}}function validateLoginForm(){const id=document.getElementById("student_id").value.trim();const pass=document.getElementById("password").value;if(!/^[A-Za-z0-9-]{3,50}$/.test(id)){alert("Enter a valid Student ID");return false}if(pass.length<6){alert("Password must be at least 6 characters");return false}return true}function validateRegisterForm(){const name=document.getElementById("name").value.trim();const email=document.getElementById("email").value.trim();const sid=document.getElementById("student_id").value.trim();const pass=document.getElementById("password").value;const cpass=document.getElementById("confirm_password").value;const phone=document.getElementById("phone").value.trim();const file=document.getElementById("profile_pic").files[0];const emailRegex=/^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;if(name.length<3){alert("Name must be at least 3 characters");return false}if(!emailRegex.test(email)){alert("Enter a real valid email like name@example.com");return false}if(!/^[A-Za-z0-9-]{3,50}$/.test(sid)){alert("Student ID can contain letters, numbers and hyphen only");return false}if(phone&&!/^(01[3-9][0-9]{8}|\+8801[3-9][0-9]{8})$/.test(phone)){alert("Enter valid Bangladeshi phone number");return false}if(file){const ok=["image/jpeg","image/png","image/webp"];if(!ok.includes(file.type)){alert("Only JPG, PNG, WEBP image allowed");return false}if(file.size>2*1024*1024){alert("Picture must be under 2MB");return false}}if(pass.length<6){alert("Password must be at least 6 characters");return false}if(pass!==cpass){alert("Passwords do not match");return false}return true}showQueryMessage();