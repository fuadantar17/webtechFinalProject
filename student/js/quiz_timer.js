const timer=document.getElementById("timer");if(timer){let seconds=parseInt(timer.dataset.minutes,10)*60;const form=document.getElementById("quizForm");const tick=setInterval(()=>{const m=Math.floor(seconds/60);const s=seconds%60;timer.textContent=`${m}:${String(s).padStart(2,"0")}`;if(seconds<=0){clearInterval(tick);alert("Time is over. Submitting quiz.");form.submit()}seconds--},1000)}